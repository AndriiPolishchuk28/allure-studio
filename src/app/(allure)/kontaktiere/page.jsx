import Contacts from '@/components/Contacts';

export const metadata = {
    title: 'Kontaktiere uns',
};

const Kontaktiere = () => {
    return (
        <main>
            <Contacts />
        </main>
    );
};

export default Kontaktiere;
